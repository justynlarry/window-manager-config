{
    "layer": "top",
    "position": "top",
    "height": 30,
    "spacing": 4,

    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": ["clock"],
    "modules-right": ["custom/updates", "custom/weather", "temperature", "cpu", "memory", "pulseaudio", "network", "backlight", "battery", "tray"],

    "sway/workspaces": {
        "disable-scroll": false,
        "all-outputs": true,
        "format": "{name}"
    },

    "sway/mode": {
        "format": "<span style=\"italic\">{}</span>"
    },

    "clock": {
        "timezone": "America/Denver",
        "format": "{:%a %b %d  %I:%M %p}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
    },

    "battery": {
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{icon} {capacity}%",
        "format-charging": " {capacity}%",
        "format-plugged": " {capacity}%",
        "format-icons": ["", "", "", "", ""]
    },

    "network": {
        "format-wifi": " {essid}",
        "format-ethernet": " {ifname}",
        "format-disconnected": "⚠ Disconnected",
        "tooltip-format": "{ifname}: {ipaddr}/{cidr}"
    },

    "pulseaudio": {
        "format": "Vol: {icon} {volume}%",
        "format-muted": " Muted",
        "format-icons": {
            "default": ["", "", ""]
        },
        "on-click": "pavucontrol"
    },

    "backlight": {
        "format": "Bright: {icon} {percent}%",
        "format-icons": ["", ""]
    },

    "tray": {
        "icon-size": 18,
        "spacing": 10
    },

    "temperature": {
        "hwmon-path": "/sys/class/hwmon/hwmon2/temp1_input",
        "ciritical-threshold": 80,
        "format": "Temp: {icon} {temperatureC}°C",
        "format-critical": " {temperatureC}°C",
        "format-icons": ["", "", ""],
        "states": {
            "good": 40,
            "warm": 60,
            "hot": 70,
            "critical": 80
        }
    },

    "cpu": {
        "interval": 2,
        "format": " CPU: {usage}%",
        "tooltip": true,
        "on-click": "alacritty -e htop"
    },

    "memory": {
        "interval": 5,
        "format": " Mem: {percentage}%",
        "tooltip-format": "{used:0.1f}G / {total:0.1f}G used"
    },

    "custom/weather": {
        "exec": "curl -s 'wttr.in/Denver?format=%t+%C' 2>/dev/null",
        "interval": 2000,
        "format": " {} ",
        "tooltip": false
    },

    "custom/updates": {
        "exec": "checkupdates | wc -l",
        "interval": 3600,
        "format": "Updates: {} ",
        "tooltip": true
    }

}
